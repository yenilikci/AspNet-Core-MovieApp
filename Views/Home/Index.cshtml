@model IEnumerable<Movie>

<div class="row">
    <div class="col-md-3"> 
        @Html.Partial("_menu")
    </div>
    <div class="col-md-9">
        <!-- @Html.Partial("_movies",Model -->
        <!--Movie listesinden dönen filmler Model olarak _movies partial'ına gönderildi-->
        <partial name="_movies" model="Model"></partial>
    </div>
</div>